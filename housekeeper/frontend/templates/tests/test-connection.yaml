apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "saashousekeeper.fullname" . }}-test-connection"
  labels:
    {{- include "saashousekeeper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "saashousekeeper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
