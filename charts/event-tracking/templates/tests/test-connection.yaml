apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "event-tracking.fullname" . }}-test-connection"
  labels:
    {{- include "event-tracking.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "event-tracking.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
