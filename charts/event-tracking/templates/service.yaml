apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-tracking.name" . }}
  labels:
    {{- include "event-tracking.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: {{ include "event-tracking.name" . }}
  selector:
    {{- include "event-tracking.selectorLabels" . | nindent 4 }}
