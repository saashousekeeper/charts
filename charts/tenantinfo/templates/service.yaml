apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenantinfo.name" . }}
  labels:
    {{- include "tenantinfo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "tenantinfo.selectorLabels" . | nindent 4 }}
