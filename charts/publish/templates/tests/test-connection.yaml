apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "publish.fullname" . }}-test-connection"
  labels:
    {{- include "publish.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "publish.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
