apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenantinfo-service.fullname" . }}
  labels:
    {{- include "tenantinfo-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tenantinfo-service.selectorLabels" . | nindent 4 }}
