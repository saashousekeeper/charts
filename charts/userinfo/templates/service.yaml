apiVersion: v1
kind: Service
metadata:
  name: {{ include "userinfo.fullname" . }}
  labels:
    {{- include "userinfo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "userinfo.selectorLabels" . | nindent 4 }}
